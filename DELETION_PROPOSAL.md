# Deletion / Ignore Proposal

This document lists files and folders that appear to be editor/CMS artifacts or generated build outputs. I recommend reviewing these and approving deletion or ignoring them in `.gitignore` where appropriate. I will not delete anything without your explicit approval.

Suggested removals or ignores

- `.astro/keystatic-imports.js` — Keystatic import shim generated by the CMS. If you are not running Keystatic in production, this is unnecessary.
- `KEYSTATIC_ENHANCEMENTS.md` — Documentation about Keystatic UI changes; keep only if you need the notes.
- `.vercel/` (output) — Generated deployment output; typically not committed. Add to `.gitignore` if present in the repo.
- `dist/` — Build output produced by `npm run build`; ignore or remove as appropriate.
- `.vercel/output/` — Build artifacts for Vercel functions; generated and safe to remove.
- `.astro/` — Local Astro cache/build artifacts; safe to ignore.
- `node_modules/` — Should not be committed; ensure it's ignored.
- `.cache/` — Build caches; safe to ignore.
- Any `keystatic.*` dev config if you are not using the CMS in production (I can list specific files if you want).

Recommended `.gitignore` additions (if not already present)

```
# Build artifacts
dist/
.astro/
.cache/

# Deployment output
.vercel/
.vercel/output/

# Node
node_modules/

# Environment
.env.local
.env.*.local

# Keystatic (if you don't use it)
keystatic.config.ts
src/styles/keystatic-ui.css

```

Next steps

- I can produce a precise list of Keystatic-related files to delete (searching for `keystatic.config.ts`, `@keystatic` imports, and CMS routes) and create a PR or branch that removes them.
- Or I can add the recommended `.gitignore` entries and leave files in place until you confirm deletion.

Which would you prefer?